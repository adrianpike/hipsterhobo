<article class="entry">
	<%=image_tag @entry.url, :alt => @entry.name %>
	
	<%=form_for [@entry,@vote] do |f| %>
		<%=f.hidden_field(:score, :value => 10) %>
		<%=f.submit "Hipster!" %>
	<% end %>
	
	<%=form_for [@entry,@vote] do |f| %>
		<%=f.hidden_field(:score, :value => 0) %>
		<%=f.submit "Hobo!" %>
	<% end %>
</article>